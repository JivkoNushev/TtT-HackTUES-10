<% @audios.each do |audio| %>
  <div>
    <%= audio.audio_file.filename %>
    <audio controls>
      <source src="<%= rails_blob_path(audio.audio_file) %>" type="audio/wav">
      Your browser does not support the audio element.
    </audio>
  </div>
<% end %>